/**
 * Enum to list all events that are propagated from component to component
 * This is to avoid magic string.
 */
enum Events {
    DELETE = 'delete',
    MOVED = 'moved',
}

export default Events;
